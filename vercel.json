{
  "installCommand": "pnpm install",
  "buildCommand": "DATABASE_URL=postgresql://fake:fake@localhost:5432/fake npx prisma generate --schema=packages/prisma/prisma/schema.prisma && pnpm --filter web build",
  "outputDirectory": "apps/web/.next",
  "framework": "nextjs"
}
