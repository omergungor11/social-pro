/**
 * NOTE: This file is intentionally NOT a Next.js page export.
 *
 * In Next.js App Router, `app/(marketing)/page.tsx` and `app/page.tsx`
 * both resolve to the "/" route and cannot coexist. The landing page lives
 * at `app/page.tsx` to avoid a duplicate-route build error.
 *
 * The (marketing) route group's layout.tsx still applies to pages nested
 * under (marketing)/ sub-directories (e.g. (marketing)/about/page.tsx).
 *
 * To migrate the landing page into this group:
 *   1. Delete `app/page.tsx`
 *   2. Move the page content here
 */

// Intentionally empty — not exported as a default page component.
export {};
